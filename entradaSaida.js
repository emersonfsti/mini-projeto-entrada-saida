// Função simulando leitura de dados do usuário
export function gets() {
    const notas = [7, 6, 8];  // Notas fixas para simular entrada (poderia ser via prompt)
    return notas;
}

// Função para imprimir saída
export function print(texto) {
    console.log(texto);
}
